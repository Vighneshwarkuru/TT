spring.application.name=firstproject

# Using embedded H2 for local development during migration
# MySQL settings are commented out; restore them if you run against MySQL in a specific profile
spring.datasource.url=jdbc:mysql://localhost:3306/firstdb
spring.datasource.username=firstuser
spring.datasource.password=First@123

# spring.datasource.url=jdbc:h2:mem:firstdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# Ensure Hibernate uses H2 dialect when running locally
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

# Note: This configuration is for local runs only. Use profile-based properties to use real DB in other environments.